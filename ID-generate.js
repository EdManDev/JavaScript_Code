// var msg = 'Name Surname ll';
// var msgUP = msg.toUpperCase(); // upercase mu string
// var acronym = msgUP.match(/\b(\w)/g).join(''); // give all first character of each word
// var acronym2 = acronym.slice(0, 2); // giv only 2 leter
// var number = Math.floor(10000 + Math.random() * 90000); // generate random number
// const user_id = acronym2 + '-' + number;
// console.log(user_id);

// console.log(acronym + '-' + number);
